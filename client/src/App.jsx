import React from 'react';import {BrowserRouter,Routes,Route} from 'react-router-dom';import Shell from './layout/Shell';import Home from './pages/Home';import ProjectDetail from './pages/ProjectDetail';import AdminPage from './pages/AdminPage';import usePortfolio from './hooks/usePortfolio';import './styles/global.css';
export default function App(){const {data,loading,refresh}=usePortfolio();return <BrowserRouter><Routes><Route path="/" element={<Shell animationKey={`${data.projects.length}-${data.skills.length}-${data.experience.length}-${data.certificates.length}`}><Home data={data}/></Shell>}/><Route path="/project/:slug" element={<Shell animationKey={`${data.projects.length}-${data.skills.length}-${data.experience.length}-${data.certificates.length}`}><ProjectDetail data={data}/></Shell>}/><Route path="/admin" element={<AdminPage data={data} onRefresh={refresh}/>}/></Routes>{loading&&<div className="mini-loader"><div/><span>SYNCING CONTENT</span></div>}</BrowserRouter>}
