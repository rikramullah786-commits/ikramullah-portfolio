import React,{useEffect} from 'react';
import {ArrowDownRight,LockKeyhole} from 'lucide-react';
import {Link} from 'react-router-dom';
import gsap from 'gsap';
export default function HeroSection({profile,skills}){
 useEffect(()=>{const tl=gsap.timeline();tl.from('.hero-kicker',{y:25,opacity:0,duration:.7}).from('.hero-word',{y:120,opacity:0,stagger:.08,duration:1.05,ease:'power4.out'},'-.35').from('.hero-note',{y:25,opacity:0,stagger:.1,duration:.7},'-.65');return()=>tl.kill()},[]);
 return <section className="hero" id="top"><div className="hero-grid"><div className="hero-copy"><p className="eyebrow hero-kicker">PORTFOLIO / 2026 / ONLINE</p><h1><span className="hero-word">I</span><span className="hero-word">BUILD</span><span className="hero-word serif">WORLDS.</span></h1><p className="hero-sub hero-note">{profile.tagline}</p><div className="hero-actions hero-note"><a className="magnetic" href="#projects">Explore my work <ArrowDownRight size={18}/></a><Link className="ghost-btn" to="/admin"><LockKeyhole size={15}/> Content cockpit</Link><a className="ghost-btn" href={profile.github||'#'} target="_blank" rel="noreferrer">GitHub ↗</a><a className="ghost-btn" href={profile.linkedin||'#'} target="_blank" rel="noreferrer">LinkedIn ↗</a><a className="ghost-btn" href={profile.resume||'#'} target="_blank" rel="noreferrer">Resume ↗</a></div></div><div className="hero-art hero-note"><div className="orbit orbit-a"/><div className="orbit orbit-b"/><div className="profile-card"><div className="card-top"><span>NO. 01</span><span>HELLO, WORLD</span></div><img src="/profile.jpeg" alt={profile.name}/><div className="card-bottom"><strong>IR</strong><span>{String(profile.role||'FULL STACK DEVELOPER').toUpperCase()}</span></div></div>{skills.slice(0,3).map((s,i)=><div key={s.id} className={`floating-tag tag-${['one','two','three'][i]}`}>{s.name} ↗</div>)}</div></div><div className="hero-bottom"><span>SCROLL TO DISCOVER</span><span>01 — 05</span></div></section>
}
