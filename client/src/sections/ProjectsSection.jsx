import React from 'react';import {Link} from 'react-router-dom';import {ArrowUpRight,Sparkles} from 'lucide-react';import gsap from 'gsap';
const imageOf=p=>p.coverImage||(Array.isArray(p.screenshots)&&p.screenshots[0]?(typeof p.screenshots[0]==='string'?p.screenshots[0]:p.screenshots[0].image):'');
export default function ProjectsSection({projects}){const onMove=e=>{const r=e.currentTarget.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;gsap.to(e.currentTarget,{rotateY:x*4,rotateX:-y*4,y:-5,duration:.3,transformPerspective:1000})};const reset=e=>gsap.to(e.currentTarget,{rotateX:0,rotateY:0,y:0,duration:.65,ease:'power3.out'});return <section className="projects section" id="projects"><div className="section-heading reveal"><div><p className="eyebrow">01 — PROJECTS & CRAFT</p><h2>Things I've <em>built.</em></h2></div><span>{projects.length} CASE STUDIES</span></div><div className="project-list">{projects.map((p,i)=>{const cover=imageOf(p);return <Link to={`/project/${p.slug||p.id}`} className={`project-card ${p.accent||''} reveal`} key={p.id} onMouseMove={onMove} onMouseLeave={reset}><div className="project-meta"><span>{p.no||String(i+1).padStart(2,'0')}</span><span>{p.type||'PROJECT'}</span><span>{p.year||''}</span></div><div className="project-main"><div className="project-visual"><div className="visual-window"><div className="window-bar"><i/><i/><i/></div>{cover?<img src={cover} alt={`${p.title} project preview`}/>:<><div className="mock-lines"><b/><b/><b/><b/></div><div className="mock-chart"><span/><span/><span/><span/><span/></div></>}</div><Sparkles className="visual-spark" size={24}/></div><div className="project-info"><p className="status">{p.status} · {p.year}</p><h3>{p.title}</h3><p>{p.description}</p><div className="chips">{(p.tech||[]).slice(0,6).map(t=><span key={t}>{t}</span>)}</div></div></div><span className="round-arrow"><ArrowUpRight size={24}/></span><div className="view-case">VIEW FULL CASE STUDY <ArrowUpRight size={15}/></div></Link>)}</div></section>}
