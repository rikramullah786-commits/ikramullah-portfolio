import React from 'react';import {ArrowUpRight,Award} from 'lucide-react';
export default function CertificatesSection({certificates=[]}){if(!certificates.length)return null;return <section className="certificates section" id="certificates"><div className="section-heading reveal"><div><p className="eyebrow">05 — CERTIFICATES</p><h2>Proof of <em>learning.</em></h2></div><span>{certificates.length} CERTIFICATES</span></div><p className="section-lead reveal">Certifications and learning milestones that support the work I build.</p><div className="certificate-grid">{certificates.map((c,i)=><article className="certificate-card reveal" key={c.id||i}><div className="certificate-image">{c.image?<img src={c.image} alt={c.title||'Certificate'}/>:<Award size={42}/>}</div><div className="certificate-body"><div className="project-meta"><span>{String(i+1).padStart(2,'0')}</span><span>{c.date||''}</span></div><h3>{c.title}</h3><p className="certificate-issuer">{c.issuer}</p>{c.description&&<p>{c.description}</p>}{c.credentialUrl&&<a className="text-link" href={c.credentialUrl} target="_blank" rel="noreferrer">View credential <ArrowUpRight size={16}/></a>}</div></article>)}</div></section>}
